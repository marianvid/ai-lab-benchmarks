# Presenter video generation — method

This study asks whether the RTX PRO 4500 Blackwell system can turn one still
portrait and one English speech track into useful presenter footage. It does
not test cinematic text-to-video. The target is deliberately narrower: a host,
guest or character speaking inside a composited discussion, explainer or
vertical story.

Each result is one deterministic run on the same machine described in
[Configuration](machine.md). The source portraits were generated by FLUX.2 Dev
32B Q8_0 through AI-Lab and remain unchanged between compatible models. The
speech tracks come from the repository's English TTS pass. For the final
TA2.0 viewing set, the documentary host uses an 8.96-second male baritone
track, the roundtable guest uses a 14.08-second female contralto track, and the
animated storyteller uses the six-second character track. Other model rows
retain their original fixtures, so wall-time comparisons use each row's stated
output duration. Models and transient runtime files stay in benchmark storage;
no video model is promoted to core by these tests.

## Cases

| Case | Still image | Speech source | Production question |
|---|---|---|---|
| Documentary host | Photorealistic male host, locked studio framing | Qwen3-TTS VoiceDesign, low warm American male baritone | Can a recurring presenter retain identity and speak naturally? |
| Roundtable guest | Photorealistic Black woman researcher, locked studio framing | Qwen3-TTS VoiceDesign, deep female contralto | Can a guest tile remain stable while showing restrained expression? |
| Animated storyteller | Older fox in polished 3D-animation style | Qwen3-TTS VoiceDesign, older Irish storyteller | Can the same face-driven route animate a non-human character? |

The exact portrait prompts, intended motion descriptions and fixed seeds are
in [`harness/video`](../harness/video/). The spoken texts remain in the English
TTS manifest so the audio and video studies use the same material.

### Audio scope

The speech files are fixed control inputs for animation and synchronization.
The final TA2.0 clips use voices selected to suit the visible host, guest and
character, but this video study does not assign a separate voice-quality score.
In a production workflow, voice casting, synthesis or conversion and audio
processing happen separately; the resulting final speech track is then
supplied to the audio-driven animation or lip-sync stage. LivePortrait Animals
is motion-driven rather than audio-driven and is not included in the
synchronization comparison.

## Models and settings

| Model | Role in the test | Settings |
|---|---|---|
| EchoMimicV3 preview 1.3B | Generative talking head with more facial motion | 512×512, 25 fps, five steps, INT8 transformer managed by mmgp, BF16 compute, CPU/GPU offload |
| LatentSync 1.6 | Lip-sync editor; changes the mouth region of an existing video | 768×768, 25 fps, 20 steps, guidance 1.5, DeepCache |
| InfiniteTalk 14B FP8 | Quality-at-the-limit generative talking head | 480p bucket, 16 fps, 40 steps, quantized T5 and DiT, zero persistent DiT parameters, CPU/GPU offload |
| MuseTalk 1.5 | Lightweight lip-sync comparison | Prepared separately; a result is reported only if the official preprocessing path runs on this machine |
| JoyVASA animal | Audio-driven animation for non-human characters | 512×512, 25 fps, CFG 2.0, animal checkpoints and English six-second speech fixture |
| LivePortrait Animals | Controlled motion transfer for non-human characters | 512×512, 25 fps, official 78-frame `d0.mp4` driving fixture, auto-cropped driver, stitching disabled as recommended for animals |
| Wan2.2-S2V 14B FP8 scaled | Native sound-to-video quality-at-the-limit comparison | 640×640, 77 frames at 16 fps, 20 steps, CFG 6.0, UniPC/simple, shift 8.0, ComfyUI dynamic offload |
| LongCat-Video-Avatar 1.5 INT8 | Generative avatar candidate with native multi-segment continuation | 480p bucket, 25 fps, eight distilled steps, INT8 DiT, sequential component loading, PyTorch SDPA |
| TA2.0 | Audio-driven 5B avatar candidate | 640×608, 25 fps, 30 steps, BF16, sequential UMT5/video offload, PyTorch SDPA |
| SkyReels V3 Talking Avatar 19B FP8 | Deliberate system-memory boundary test | 480p, FP8 weight-only low-VRAM mode, block offload; attempted with 48 GiB and 64 GiB container RAM |

EchoMimicV3 uses upstream commit
`7e89489ca51c0d008fc1963ec6c03fc5bd0b9397`; LatentSync uses
`a229c3948406bc2cf6eaf4873e662e70c6a04746`; InfiniteTalk uses
`50aa0a94184315407a991ae804d9b58d6d311ba8`; MuseTalk uses
`8ca7d1884cf5c1c766dcd0365b069c96d75707cf`; JoyVASA uses
`916a90f8de490e8648fee460c1200bd5d9a795af`; LivePortrait uses
`9b294b3d0536135442ea73cb01e6cb3ca7029dd3`; the native Wan2.2-S2V run uses
ComfyUI commit `77739723a36ea503f875c121b03e9c4288aa4914` (ComfyUI 0.34.0).
LongCat-Video uses commit `6b3f4b8582a8bc3f20f795735f5383716c4ba794`;
TA2.0 uses `56fe45a61e33b73294710171f0604d3f432080b8`; SkyReels V3 uses
`28c771e8456341be6a213e3d1133ed1fd19bf75d`.

InfiniteTalk needs two compatibility changes on this Blackwell runtime. The
quantization extension is compiled against the installed CUDA 13 toolchain,
and FlashAttention/xFormers calls whose published kernels reject compute
capability 12.0 use PyTorch scaled dot-product attention instead. The source
patch is preserved in [`harness/video/patches`](../harness/video/patches/).
This fallback is expected to be slower than a native Blackwell attention
kernel and is therefore part of the practical result.

LongCat also uses PyTorch SDPA because both its base and avatar attention
modules otherwise require an unavailable FlashAttention/xFormers kernel. The
published low-memory runner from upstream pull request 115 loads the text,
audio, VAE and INT8 DiT stages sequentially. Its optional FP8 conversion is not
used in the reported run: conversion temporarily reconstructs BF16 weights and
exceeded the 32 GB GPU before inference. ONNX Runtime 1.23.2 replaces the
upstream 1.16.3 pin because the older wheel requests an executable stack that
the container correctly refuses. NumPy remains pinned to 1.26.4.

TA2.0 resolves its declared video and audio dependencies from complete local
benchmark directories. Its released pipeline keeps the video stack resident
while constructing UMT5-XXL, exceeding 32 GB. The compatibility patch moves
the video, VAE and audio components to system memory only while the prompt is
encoded, then restores them to the GPU. A second patch routes the model through
its existing PyTorch SDPA fallback when FlashAttention is unavailable. The
fallback warns that its padding mask is disabled; this is part of the tested
configuration and may affect performance on padded chunks.

SkyReels was attempted twice with its official `--low_vram --offload` path.
The first run exhausted 48 GiB RAM plus 8 GiB swap. A second boundary run raised
the container to 64 GiB RAM with the same 8 GiB swap, but model construction
was still killed before generation at 62.9 GiB observed process RSS. The
container was restored to its normal 48 GiB limit afterward. No smaller model
or substituted precision is reported as SkyReels V3.

JoyVASA and LivePortrait Animals use the same reference PyTorch XPose fallback
because the published custom extension cannot be built against this CUDA
13 host and the CUDA 12.8 PyTorch wheel. LivePortrait reuses the complete
checkpoint tree already present under benchmark storage; it does not create a
second or partial model copy.

Wan2.2-S2V is loaded by ComfyUI directly from the complete FP8-scaled model
tree in benchmark storage. The published checkpoint carries ComfyUI
quantization metadata, so the native loader is part of the tested
configuration. A preliminary DiffSynth compatibility experiment produced a
valid container with visually corrupted frames and is excluded from the
viewing set; the corrective native run is the reported result. Its exact API
workflow is stored as [`wan22-s2v-comfyui-api.json`](../harness/video/wan22-s2v-comfyui-api.json).

## Measurements

Wall time covers model initialization, generation and output encoding. Peak
GPU memory is polled through `nvidia-smi`; process resident memory is polled
from the operating system. These are observed peaks, not allocator guarantees.

An InsightFace pass samples frames at the interval recorded in each result JSON
and reports:

- face detection rate;
- mean and minimum cosine similarity to the source portrait;
- normalized face-centre and face-area variation;
- Laplacian sharpness;
- mean pixel change between sampled frames.

The identity cosine values compare each generated face with its own source.
They are useful for comparing two treatments of the same portrait, not as a
universal quality threshold. Low frame change means stability, but it can also
mean insufficient expression.

The official SyncNet evaluator reports audio-video offset and confidence where
its detector accepts the clip. This is an automatic lip-sync signal, not a
claim about natural expression, voice-character suitability or overall
production quality.

## Automatic verdicts

The report assigns practical automatic verdicts from successful generation,
face coverage, identity retention, synchronization and measured resource cost.
The accompanying video page carries no viewing score: it presents the input
and output so each reader can inspect motion, lip shape and visual artifacts
directly.

## Licences

The checked-out EchoMimicV3, LatentSync, InfiniteTalk, LivePortrait and TA2.0
repositories carry Apache-2.0 licences. MuseTalk, JoyVASA and LongCat carry MIT
licences; ComfyUI carries GPL-3.0, and the Wan2.2 repository carries
Apache-2.0. SkyReels V3 uses the Skywork Community License, which permits
commercial use subject to its custom terms. Base models and dependencies may
add separate terms; production use must follow the model cards and every
redistributed component, not only the runtime repository licence.
