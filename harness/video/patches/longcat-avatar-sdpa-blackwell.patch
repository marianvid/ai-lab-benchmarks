--- a/longcat_video/modules/attention.py
+++ b/longcat_video/modules/attention.py
@@ -2,6 +2,7 @@
 
 import torch
 import torch.nn as nn
+import torch.nn.functional as F
 
 from einops import rearrange
 
@@ -111,7 +112,7 @@
             x = xformers.ops.memory_efficient_attention(q, k, v, attn_bias=None, op=None,)
             x = rearrange(x, "B M H K -> B H M K")
         else:
-            raise RuntimeError("Unsupported attention operations.")
+            x = F.scaled_dot_product_attention(q, k, v, scale=self.scale)
 
         return x
 
@@ -244,7 +245,14 @@
             import xformers.ops
             attn_bias = xformers.ops.fmha.attn_bias.BlockDiagonalMask.from_seqlens([N] * B, kv_seqlen)
             x = xformers.ops.memory_efficient_attention(q, k, v, attn_bias=attn_bias)
         else:
-            raise RuntimeError("Unsupported attention operations.")
+            q_bhsd = rearrange(q, "B S H D -> B H S D")
+            k_bhsd = rearrange(k, "B S H D -> B H S D")
+            v_bhsd = rearrange(v, "B S H D -> B H S D")
+            x = F.scaled_dot_product_attention(
+                q_bhsd, k_bhsd, v_bhsd, scale=self.head_dim**-0.5
+            )
+            x = rearrange(x, "B H S D -> B S H D")

--- a/longcat_video/modules/avatar/attention.py
+++ b/longcat_video/modules/avatar/attention.py
@@ -2,6 +2,7 @@
 
 import torch
 import torch.nn as nn
+import torch.nn.functional as F
 
 from einops import rearrange
 
@@ -111,7 +112,7 @@
             x = xformers.ops.memory_efficient_attention(q, k, v, attn_bias=None, op=None,)
             x = rearrange(x, "B M H K -> B H M K")
         else:
-            raise RuntimeError("Unsupported attention operations.")
+            x = F.scaled_dot_product_attention(q, k, v, scale=self.scale)
 
         return x
 
@@ -426,6 +427,10 @@
             encoder_v = rearrange(encoder_v, "B H M K -> B M H K")
             x = xformers.ops.memory_efficient_attention(q, encoder_k, encoder_v, attn_bias=None, op=None,)
             x = rearrange(x, "B M H K -> B H M K") 
+        else:
+            x = F.scaled_dot_product_attention(
+                q, encoder_k, encoder_v, scale=self.scale
+            )
 
         # linear transform
         x_output_shape = (B, N, C)
--- a/run_demo_avatar_single_lowmem.py
+++ b/run_demo_avatar_single_lowmem.py
@@ -216,6 +216,12 @@
     config.pop("_diffusers_version", None)
     config.pop("model_max_length", None)
     config["cp_split_hw"] = cp_split_hw
+    # Published FlashAttention kernels do not support this Blackwell runtime.
+    # Force the PyTorch SDPA fallback patched into the attention modules.
+    config["enable_flashattn3"] = False
+    config["enable_flashattn2"] = False
+    config["enable_xformers"] = False
+    config["enable_bsa"] = False
 
     # Build model skeleton with meta tensors to avoid RAM allocation
     with torch.device("meta"):
